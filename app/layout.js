export const metadata = {
  title: 'Jayson Quindao | Autonomous Enterprise Scale',
  description: 'Executive Portfolio',
};

import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
